## Summary

What this PR changes and why. Link the issue it closes (Closes #N) if any.

## Changes

- ...

## Testing

How the change was verified:

- [ ] `go test ./...` and `go vet ./...`
- [ ] Relevant tagged suites: `make test-functional` / `make test-integration` (if touched)
- [ ] Frontend checks if `portal-frontend/` is touched (`npm run typecheck`, `npm test`)

## Changelog

- [ ] User-facing change: entry added to the `[Unreleased]` section of `CHANGELOG.md`
- [ ] Internal-only change: no changelog entry needed
