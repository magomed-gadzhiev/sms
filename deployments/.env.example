# Sample environment file for SMS Platform docker-compose deployment.
# Copy to deployments/.env and replace generated values before use.

POSTGRES_PASSWORD=change-me-generate-via-openssl-rand-base64-32
REDIS_PASSWORD=change-me-generate-via-openssl-rand-base64-32
JWT_SECRET=change-me-generate-via-openssl-rand-base64-64
KAFKA_EXTERNAL_HOST=localhost
