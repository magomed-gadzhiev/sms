# Sample environment file for sms platform docker-compose deployment.
# Copy to .env and replace REDIS_PASSWORD with a strong password (openssl rand -base64 32).
# REDIS_PASSWORD is required — docker-compose will refuse to start without it.

REDIS_PASSWORD=changeme-generate-via-openssl-rand-base64-32
