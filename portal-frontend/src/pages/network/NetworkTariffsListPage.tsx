// TODO(task-9): replace stub with sub-accounts tariff summary table
// (see docs/superpowers/plans/2026-04-22-network-tariffs-redesign.md §Task 9).
// Also hosts the legacy `?tab=*` redirect per Task 10.
export function NetworkTariffsListPage() {
  return <div>TODO: NetworkTariffsListPage</div>;
}
